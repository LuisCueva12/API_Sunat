# Operación

Pendiente — se completa en Fase 9 de [01_ARQUITECTURA.md](01_ARQUITECTURA.md): runbooks de incidentes (SUNAT caído, certificado vencido, cola atascada), backups (PostgreSQL, XML/CDR, certificados cifrados, configuración) y política de retención (comprobantes emitidos nunca se eliminan físicamente — desactivar/anular/dar de baja son operaciones distintas de `DELETE`).
